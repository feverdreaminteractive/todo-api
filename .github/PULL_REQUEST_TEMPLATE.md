Relates to #

## Summary

<!-- What changed, and why. -->

## How to test this PR

```sh
git fetch origin
git checkout <branch>
npm install
npm test
```

## Verification

- `npm test` —
- `npm run lint` —
- `npm run build` —

<!-- Add manual verification notes (curl output, browser check, docker build, etc.)
     for anything the automated suite doesn't cover directly. -->
